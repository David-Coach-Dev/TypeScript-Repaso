export const name = "Dc Dev";
export const age = 40;
export const isValid = true;

export const templateString =`esto es un string
multilineal
que se ouede tenre
" dobles
' simple
inyectar varia bles ${name}
\s hola
expresiones ${1 + 1}
números ${age}
boolea s{isValid}`;
console.log(templateString);